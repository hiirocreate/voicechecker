# Add project specific ProGuard rules here.
# This app ships with minifyEnabled=false by default (personal-use build),
# so these rules only take effect if you later enable R8/ProGuard.

-keep class com.vocalrange.analyzer.data.** { *; }
